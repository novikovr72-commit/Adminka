{
  "name": "frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "sh -c 'PID=$(lsof -tiTCP:5175 -sTCP:LISTEN); if [ -n \"$PID\" ]; then kill -9 $PID; fi; vite'",
    "dev:fresh": "sh -c 'PID=$(lsof -tiTCP:5175 -sTCP:LISTEN); if [ -n \"$PID\" ]; then kill -9 $PID; fi; vite --force'",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tanstack/react-virtual": "^3.13.23",
    "prismjs": "^1.30.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.1",
    "vite": "^7.3.1"
  }
}
